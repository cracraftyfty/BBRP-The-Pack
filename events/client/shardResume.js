//here the event starts
module.exports = (client, id, replayedEvents) => {
    console.log(`[ThePack]  || <==> || [${String(new Date).split(" ", 5).join(" ")}] || <==> || Shard #${id} Resumed || <==> ||`)
}
